import { EntityForm } from "@/components/management/entity-form"; import { Field,SelectField } from "@/components/management/fields"; import { RecordList } from "@/components/management/record-list"; import { saveMember } from "@/lib/management/actions"; import { prisma } from "@/lib/prisma";
export default async function Page({searchParams}:{searchParams:Promise<{edit?:string}>}){const {edit}=await searchParams;const [records,plans,current]=await Promise.all([prisma.member.findMany({include:{plan:true},orderBy:{name:"asc"}}),prisma.plan.findMany({orderBy:{name:"asc"}}),edit?prisma.member.findUnique({where:{id:edit}}):null]);return <Wrap title="Clientes"><EntityForm action={saveMember}><input type="hidden" name="id" value={current?.id??""}/><Field label="Nombre" name="name" defaultValue={current?.name}/><Field label="Correo" name="email" type="email" defaultValue={current?.email}/><Field label="Teléfono" name="phone" required={false} defaultValue={current?.phone??""}/><SelectField label="Estado" name="status" defaultValue={current?.status} options={[["ACTIVE","Activo"],["INACTIVE","Inactivo"],["OVERDUE","Moroso"]]}/><SelectField label="Plan actual" name="planId" required={false} defaultValue={current?.planId??""} options={plans.map(x=>[x.id,x.name])}/></EntityForm><RecordList kind="member" rows={records.map(x=>({id:x.id,title:x.name,details:`${x.email} · ${x.plan?.name??"Sin plan"} · ${x.status}`}))}/></Wrap>}
function Wrap({title,children}:{title:string;children:React.ReactNode}){return <><header className="mb-8 border-b-2 border-[var(--ink)] pb-6"><h1 className="font-display text-5xl font-extrabold">{title}</h1><p className="mt-2 text-[var(--muted)]">Personas inscritas y su situación actual.</p></header><div className="space-y-6">{children}</div></>}
